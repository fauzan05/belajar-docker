docker compose build # jika hanya membuild image dari dockerfile saja

docker compose start # create container, kemudian menjalankan kontainernya

docker container exec -i -t app /bin/sh

docker compose up # untuk menjalankan container sekaligus create ulang container